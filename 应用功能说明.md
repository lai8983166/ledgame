# LED Game 应用功能说明

本文档说明当前应用中已经可用的主要功能和操作方式。

## 进入游戏

“进入游戏”用于验证完整的玩家游戏流程。实际进入的流程由“配置”中的“进入游戏方式”和“模式”决定。

- 调试模式：打开 Touch 界面和 Debug Panel。两个窗口会按照当前显示器的工作区左右分屏，Touch 在左侧，Debug Panel 在右侧，各占一半区域，并适配不同分辨率和 DPI。
- 游戏模式：只打开全屏 Touch 界面，不显示 Debug Panel。
- 触屏方式：在 Touch 界面上进行操作。
- 投币方式：等待投币输入后进入游戏配置。
- 手环方式：读取手环后进入游戏配置；目前只读取并显示手环 ID，余额查询尚未接入。

游戏模式的基本流程为：待机、选择人数、选择游戏、选择起始关卡、倒计时、游戏运行、结算。Touch 界面会根据当前阶段显示对应的动画和文字。

Touch 界面在游戏模式下负责玩家操作和游戏阶段展示；Debug Panel 只用于调试模式下输入点击、查看运行状态和测试玩法。

## Demo（只能搭配小灯板使用）

“Demo”是快速验证入口，用于直接启动测试用的 Simple 游戏。它跳过完整的玩家配置流程，直接进入调试运行路径，适合快速验证帧、玩法、踩踏输入和 LED 输出。

## 游戏列表

游戏列表展示当前可编辑的游戏。当前 Simple 玩法入口包括 `simple-demo`、`simple`、`normal` 和 `diffcult`，它们使用同一套 Simple 编辑器和玩法运行逻辑，但读取不同的游戏数据。

- 点击游戏卡片进入编辑器。
- 点击卡片右下角的编辑图标，打开“修改游戏信息”表单。
- 当前可以修改游戏封面。选择图片后保存，封面会按 `183 x 308` 的容器尺寸适配并持久化保存。

### 帧

- 点击帧编号切换当前帧。
- 点击“添加帧”在末尾新增帧。
- 点击“在当前帧后添加帧”在当前帧后插入新帧。
- 点击“复制当前帧到前一帧”“复制当前帧到后一帧”或“复制当前帧到所有帧”复制帧内容。
- 复制到已有帧可能覆盖原内容，操作前会弹出确认框；取消后不会修改目标帧。
- 点击“删除当前帧”删除当前帧，删除前会弹出确认框。
- 使用 `A`、`D` 键切换帧，也可以拖动帧进度区域切换。
- 使用帧导入按钮导入 JSON 并替换当前帧，使用帧导出按钮导出当前帧 JSON。
- 输入“重复”设置当前帧的重复次数，点击旁边的应用按钮可应用到当前关卡的所有帧。

### 基础信息

- 编辑游戏名称、描述、备注、矩阵宽度和矩阵高度。
- 设置“关卡时间限制”：无限制、循环 n 次或循环 n 秒。选择循环方式后，需要填写对应数值。
- 设置“关卡生命值”：无限制或有限制。选择有限制后，需要填写生命值。
- 点击“全局配置”配置游戏使用的音频、GIF 等全局媒体资源。
- 点击“保存”持久化游戏数据。
- 点击“校验”检查游戏数据是否满足导入和运行要求。

关卡序列区域提供以下操作：

- 点击“添加关卡”新增关卡。
- 点击“删除当前关卡”删除当前关卡，删除前会弹出确认框。
- 点击“关卡前移”或“关卡后移”调整关卡顺序。

### 矩阵编辑

- 在“新增对象”模式下，左键点击空白格创建单格对象；左键拖拽可以框选区域并创建多格对象。
- 在“选择/移动”模式下，左键点击对象进行选择，拖拽对象进行移动，也可以使用方向键移动。
- 在选择/移动模式下，颜色按钮不可用；切换回新增对象模式后才能选择颜色。
- 点击“矩阵编辑”或“全景编辑”切换编辑范围。全景编辑可以查看和编辑 RGB 区域之外的对象。
- 在全景编辑中可以选择多个同色对象并合并，合并后保留为一个对象。
- 在关卡名称输入框旁边都的"重复"可以设置每帧的重复次数，旁边的\*按钮可将本帧重复次数应用到所有帧
- 使用鼠标滚轮调整矩阵缩放。
- Simple 的重叠规则不使用层级数值决定显示优先级：后加入的对象通常覆盖先加入的对象，但绿色对象具有最高显示和点击优先级，绿色对象存在时不会被其他颜色对象覆盖。
- 直接使用全景按钮旁的撤销/重做按钮或者使用 `Ctrl+Z` 撤销、`Ctrl+Y` 或 `Ctrl+Shift+Z` 重做。撤销/重做目前只针对 RGB 区域的编辑操作。

### 对象编辑

- 点击矩阵中的对象，或点击“对象”列表中的对象，选中对象。
- 点击“左转90”或“右转90”旋转对象。
- 点击“修改基准”后选择新的基准点并确认。
- 使用层级加减按钮调整对象层级元数据；这不会改变绿色对象的显示优先级规则。
- 点击“改色”将对象修改为当前画笔颜色。
- 点击“复制前帧”“复制后帧”或“复制全帧”将对象复制到其他帧。
- 点击“绿色到全帧”“蓝色到全帧”或“粉色到全帧”复制对应颜色对象。
- 点击“删除”删除选中对象。
- 可能覆盖已有对象的复制操作，以及删除操作，都会先显示确认框。

### 对象

显示当前帧中的对象列表、颜色、位置、层级和占用格数。

点击列表中的对象即可选中它，也可以在矩阵区域中直接点击对象。

### 画笔

- 在“新增对象”和“选择/移动”之间切换编辑模式，快捷键为 `Q`。
- 点击 Color 0、Color 1、Color 2 或 Color 3 选择当前画笔颜色。
- 当前编辑区域会显示画笔颜色、矩阵尺寸、缩放比例和对象数量。
- 点击“启动游戏”快速启动当前游戏，用于验证当前游戏和激活关卡的运行效果。
- 点击“停止游戏”停止游戏运行。
- 点击“打开预览”播放当前关卡的只读帧序列。预览显示当前帧/总帧数和当前重复次数/总重复次数，不启动游戏，也不打开 Debug Panel。
- 点击“导出 GIF”导出当前关卡的 GIF。

## 媒体库

媒体库用于浏览应用中的媒体资源。

- 左侧选择 `media` 目录或子目录。
- 选择文件后，右侧显示预览。
- 图片直接预览，视频使用视频播放器预览，音频使用横向音频播放器预览。
- 不能预览的文件会显示不支持预览的提示。
- 使用刷新功能重新读取媒体目录。

## 精灵库

精灵库用于浏览和管理游戏编辑器使用的精灵资源。选择资源后可以查看其基本信息，并在编辑器中使用对应资源。

## 语言

“语言”用于切换界面语言。当前支持中文、英文、俄语、韩语和日语，每个选项带有对应国旗图标。

点击语言选项后，界面文字会立即切换；Touch 待机界面的提示文字也会同步使用当前语言对应的内容。

## 副屏

“副屏”是一个下拉菜单，包含“副屏列表”和“打开副屏”。

- 打开“副屏列表”查看当前机器识别到的显示器，并选择用于显示的副屏。
- 点击“打开副屏”在选中的显示器上打开副屏界面。
- 副屏用于展示游戏运行状态、得分、生命值、当前阶段和最终结果等信息，不用于编辑游戏或操作 Debug Panel。

## 配置

配置用于设置进入游戏的方式、Touch 模式以及 Touch 待机提示。

- “进入游戏方式”：触屏、投币或手环，默认是触屏。
- “Touch 待机文字”：设置待机界面中央显示的提示文字。
- “待机文字大小”：设置提示文字大小，范围为 32 到 200 像素。
- “模式”：调试或游戏，默认是调试。
- 点击“保存”保存配置。

Touch 待机界面会播放待机动画和音乐，并显示可配置的提示文字。提示文字会定期进行渐变消失动画；进入游戏配置后，待机音乐会持续到配置阶段结束。

## 帮助

“帮助”是一个下拉菜单，当前包含“调试助手”和“数据更新”。

### 调试助手

打开“调试助手”后，可以在“配置”“布线工具”和“调试工具”三个面板之间切换。

#### 配置

- “TCP 服务端口”：设置上层游戏程序连接 SDK 的端口，默认 `3002`。
- “允许重复按下”：设置同一地砖是否允许连续触发按下事件，默认关闭。
- “防抖时长 (ms)”：设置按下事件的防抖时间，默认 `50` 毫秒。
- “网卡”：选择 SDK 使用的电脑网卡。
- “控制器型号”：选择 HC08 或 HC04，默认 HC08。
- “RGB 线序”：选择 RGB 颜色线序，默认 RGB。
- 点击“下载 conf 文件”生成 SDK 配置 JSON。
- 修改外部配置后，点击“重新加载配置”让应用重新读取配置，不需要重启应用。

应用启动时不会因为网卡或控制器不可用而失败；网卡绑定和硬件检查在搜索控制器或开始调试时执行。

#### 布线工具

- 设置“画面尺寸”的宽和高，默认 `16 x 32`。
- 设置“每通道最大点数”，默认 `64`。
- 在“布线模式”中选择逐行优先、逐列优先、行蛇形或列蛇形，四种模式互斥。
- 在 Canvas 中左键点击点位可以添加单个点位；控制器的第一个点位使用四边形表示，后续点位使用六边形表示。
- 左键拖拽框选区域时，先跳过已经占用的点位，再按当前布线模式排序添加未占用点位。
- 右键点击已占用点位可以删除点位；右键拖拽可以删除选中区域内的点位。
- 同一通道的点位会按顺序连接；达到每通道最大点数后，新增点位会自动分配到下一个通道。
- “通道列表”显示每个通道和对应的点位数量。
- 点击“下载 wiring 文件”生成 wiring JSON。

#### 调试工具

- 选择 RGB 线序、网卡和控制器型号。
- 点击“搜索”按照协议搜索已连接的控制器；搜索结果会以控制器 MAC 地址显示在“控制器列表”中。
- 设置控制器数量、每通道最大点数、显示颜色和帧间隔。
- 在“单点坐标”中输入坐标，点击“测试单点”发送单点测试报文。
- 点击“开始”开始发送调试报文，点击“停止”停止发送。
- “协议日志”显示报文时间、发送或接收方向、报文类型和十六进制内容。
- 较长的报文默认折叠为一行，点击省略号按钮查看完整内容。
- 点击“清空日志”清除协议日志。

日志中的“发送”表示应用已经完成 socket 发送，不代表控制器一定已接收或地砖一定已经点亮。接收报文中主要关注控制器状态和地砖踩踏相关数据。

### 数据更新

“数据更新”用于将当前应用包中的种子数据库更新到用户数据目录。

- 更新前会自动备份当前数据库。
- 当前采用全量更新，不判断字段级增量；更新后以应用包中的种子数据库替换当前数据库。
- 更新完成后需要按提示重新加载或重启相关运行时，使新数据生效。
- 普通启动和升级不会自动覆盖用户数据，只有明确点击“数据更新”才会执行替换。
